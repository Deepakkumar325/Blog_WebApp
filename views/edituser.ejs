<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <%-include('layout/header')%>
    <div class="container">
     <div class="container"><div class="row">
        <div class="col-lg-6 mx-auto mt-4">
            <div class="card shadow">
                <div class="card-header bg-primary bg-success">
                    <div class="text-light">Edit Post  (<%=users.name%>)</div>
                </div>
                <div class="card-body p-3">
                    <form action="/update/<%=users.id%>?_method=PATCH" method="post" id="add-form" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label for="name">Name</label>
                            <input type="text" name="name"  value="<%=users.name%>" class="form-control form-control-lg" placeholder="Enter Name" required>
                        </div>
                        <div class="mb-3">
                            <label for="email">Email</label>
                            <input type="email" name="email"  value="<%=users.email%>" class="form-control form-control-lg" placeholder="Enter email" required>
                        </div> <div class="mb-3">
                            <label for="phone">Text</label>
                            <input type="phone" name="phone"  value="<%=users.phone%>" class="form-control form-control-lg" placeholder="Enter phone" required>
                        </div> <div class="mb-3">
                            <label for="image">Select Img</label>
                            <input type="file" name="image" class="form-control " placeholder="Enter Name" required>
                            <img src="/<%=users.image%>" width="100px" class="img-thumbnail mt-1">
                        </div>
                        <input type="hidden" name="old_images" value="<%=users.image%>">
                        <div class="mb-3 d-grid">
                            <input type="submit" name="submit" value="Update Post" class="btn btn-primary btn-lg bg-success">
                        </div>
                    </form>
                </div>
            </div>
        </div> 
     </div></div>
</div>
<%- include('layout/footer') %>

    
</body>
</html>